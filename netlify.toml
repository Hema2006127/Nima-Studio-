# Netlify detects Next.js and applies its OpenNext adapter automatically
# (don't pin @netlify/plugin-nextjs — Netlify keeps it up to date).
[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "22"
